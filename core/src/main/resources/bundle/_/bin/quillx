#!/bin/bash

source "$(dirname $0)/../dist/bin/qinit"

source "$Q_DIST_CORE/qpre"
source "$Q_DIST_CORE/qcore" run packages/default/muscaa@quill bin.quillx $@

POST_COMMAND=$?
source "$Q_DIST_CORE/qpost" "$Q_DIST_CORE/qcore" run packages/default/muscaa@quill bin.quillx $@
