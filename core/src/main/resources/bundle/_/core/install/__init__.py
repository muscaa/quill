import sys
from pathlib import Path

def install(package_zip: Path, destination: Path):
    pass
    # TODO
    # sys.path.insert(0, str(package_path))
    
    # exec(f"from {module_name} import main")
    # exec(f"main()")
