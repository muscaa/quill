import sys
from pathlib import Path

def install(package_path: Path, module_name: str):
    pass
    # TODO
    # sys.path.insert(0, str(package_path))
    
    # exec(f"from {module_name} import main")
    # exec(f"main()")
